import { BoardGridDirective } from './board-grid.directive';

describe('BoardGridDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardGridDirective();
    expect(directive).toBeTruthy();
  });
});
